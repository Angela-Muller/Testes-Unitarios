import { soma } from "../src/calculadora";

test("deve somar dois números corretamente", () => {
  expect(soma(2, 3)).toBe(5);
});

test("deve somar dois números corretamente", () => {
  expect(soma(2, 3)).toBe(5);
});

test("deve somar dois números corretamente", () => {
  expect(soma(2, 3)).toBe(5);
});

test("deve somar dois números corretamente", () => {
  expect(soma(2, 3)).toBe(5);
});

test("deve somar dois números corretamente", () => {
  expect(soma(2, 3)).toBe(5);
});

test("deve somar dois números corretamente", () => {
  expect(soma(2, 3)).toBe(5);
});